'use strict';
// ============================================================
// Copy to js/config.js and fill in after creating your Supabase
// project and Vercel deployment. With nulls, the game runs fully
// offline: leaderboards show OFFLINE, everything else works.
// ============================================================
window.KP_CONFIG = {
  SUPABASE_URL: null,      // e.g. 'https://abcdefgh.supabase.co'
  SUPABASE_ANON_KEY: null, // Settings -> API -> anon public key (safe to ship)
  API_BASE: null,          // e.g. 'https://kernel-panic.vercel.app' (usually same origin: '')
};
